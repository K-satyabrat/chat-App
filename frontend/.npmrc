# Ignore platform-specific dependencies
platform=linux
arch=x64

# Use legacy peer deps to avoid some dependency conflicts
legacy-peer-deps=true

# Ensure we're using the latest npm
engine-strict=true 