{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "A modern, full-stack real-time chat application built with React, Node.js, and Socket.IO.",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && cd frontend && node ./node_modules/vite/bin/vite.js build",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}